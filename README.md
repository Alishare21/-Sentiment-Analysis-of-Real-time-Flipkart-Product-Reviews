# Flipkart Review Sentiment Analyzer 🛍️

This project predicts the **sentiment (Positive/Negative)** of Flipkart product reviews using a Machine Learning model.

## 🚀 Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## 📦 Files
- `model.pkl` -> trained ML pipeline (TF-IDF + Logistic Regression)
- `app.py` -> Streamlit web app
- `requirements.txt` -> dependencies
